import java.util.ArrayList;
import java.util.Calendar;
import java.util.Comparator;
import java.util.concurrent.Executor;

import app.ATM;

public class Main {

    public static void main(String[] args) {

        // ATM.generateObjectFile();

        // ATM atm = new ATM();

        // atm.showAllAccounts();

        // atm.recharge(100);
        // atm.withdraw(35);
        // atm.transfer("abc", 50);

        // atm.showAllAccounts();
        
        // atm.logout();

        // atm.login("tanthanh", "12345678");

        // atm.logout();

        // atm.recharge(100);
        // atm.withdraw(35);
        // atm.withdraw(50);
        // atm.transfer("abc", 50);
        // atm.transfer("hoangphuc", 100);

        // atm.showAllAccounts();

        /*
         * Abstraction dùng để gom nhóm các Entity (được cho là cùng tính chất), Entity có thể là loại `Entity thuần về
         * dữ liệu (String, ...)` hoặc `Entity thuần về tính năng (Scanner, ...)`.
         * Interface dùng để `đại diện cho một phần tính năng nhỏ hỗ trợ trong một tính năng lớn (Comparator)` hoặc
         * `đại diện cho khả năng nào đó trên một Entity (Comparable)`.
         */

    }

}
